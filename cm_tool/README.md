# flask_demo

## run

```
注：~代表workspace

server:
    cd ~/flask_demo
    python app.py
    access:
        ip:port
web:
    cd ~/web
    debug: 
        npm start
    build: 
        npm run build
        cp ~/web/dist/* ~/flask_demo/static
    access:
        localhost:8989

db config:
    ~/flask_demo/init.py 
    
```
